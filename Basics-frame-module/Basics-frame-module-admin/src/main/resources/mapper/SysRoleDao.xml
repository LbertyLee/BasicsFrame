<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lh.frame.admin.dao.SysRoleDao">




    <select id="getRolesListByUserId" resultType="java.lang.String" parameterType="string">
        SELECT
            r.role_key
        FROM
            frame_sys_role AS r
                LEFT JOIN frame_sys_user_role AS ur ON r.role_id = ur.role_id
                LEFT JOIN frame_sys_user AS u ON u.user_id = ur.user_id
        WHERE
            u.user_id =#{userId}
    </select>
</mapper>